# Python dependencies for virtmcu tooling and testing.
# Legacy file. Primary dependencies are now managed in pyproject.toml via uv.
# Install via: uv sync

# QMP client — async JSON-over-socket protocol for controlling QEMU
qemu.qmp==0.0.3

# Robot Framework — test harness (Renode keyword compatibility)
robotframework==7.0.0

# Lark — EBNF parser used by tools/repl2qemu/parser.py for .repl files
lark==1.1.9

# Zenoh — federation bus used by the FirmwareStudio time authority
#
eclipse-zenoh==1.9.0

# Development / testing
pytest==8.0.0
pytest-asyncio==0.23.5
pyyaml==6.0.1
hypothesis==6.152.1
ruff==0.15.11

# MCP SDK - used for the AI Debugging interface
mcp==1.0.0

# FlatBuffers - schema-driven zero-copy serialization
flatbuffers==25.12.19

# Test timeout enforcement — prevents infinite hangs in CI
pytest-timeout==2.4.0
psutil
