[tool.ruff]
line-length = 120
exclude = [
    "third_party/",
    ".venv/",
    "**/__pycache__/",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",   # line-too-long — acceptable in a C/embedded project with long strings
]
